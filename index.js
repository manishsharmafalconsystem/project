const name = "dronna";

if (name == "demo") {
     console.log("success your data have been configurations");

     for (let index = 0; index < array.length; index++) {
          const element = array[index];
     }
} else {
     console.log("data not fetched from sever and please configurations and try again");
}